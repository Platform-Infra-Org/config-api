# Core dependencies
fastapi>=0.115.6
pydantic>=2.0.0
pydantic-settings>=2.0.0
uvicorn>=0.22.0
loguru>=0.7.0

# MongoDB-backed config store + in-memory caching
pymongo[srv]>=4.9.0
aiocache>=0.12.0

# Shared FastAPI app factory and connectors
tashtiot-apis-library==0.0.1.dev1

# Test tooling
pytest==7.4.3
httpx>=0.27.2
pytest-cov==5.0.0
pytest-mock==3.6.0
